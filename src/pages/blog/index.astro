---
import Layout from "@/layouts/Layout.astro";
import Card from "@/components/Card.astro";
import { getSortedBlogCollection } from "@/utils/ContentUtils";
const blogEntries = await getSortedBlogCollection();
---

<Layout title="极速蜗牛的博客">
  <main>
    <h1>文章列表</h1>
    <p>看看我都写了哪些文章</p>
    <ul role="list" class="link-card-grid">
      {
        blogEntries.map((post) => (
          <Card
            id={post.id}
            title={post.data.title}
            body={post.data.description}
          />
        ))
      }
    </ul>
  </main>
</Layout>

<style>
  main {
    margin: auto;
    padding: 1.5rem;
  }

  h1 {
    font-size: 3rem;
    font-weight: 800;
    margin: 0;
  }

  .link-card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
    gap: 1rem;
    padding: 0;
  }
</style>
